# PayCraft Phase 4 — publish-secret verdict.
# METADATA ONLY. No values, no head/cat/base64 (RULE-SECRETS-NO-VALUE-EGRESS-001).
# Presence = the vault resolver succeeded AND produced a non-empty file. Nothing is read.
generated_at: "2026-09-05T09:52:52Z"
registry_group: maven-central
scopes:
  framework-maven-central-username: present
  framework-maven-central-password: present
  framework-gpg-signing-key: present
  framework-gpg-signing-password: present
