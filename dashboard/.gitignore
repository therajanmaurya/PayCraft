node_modules/
.next/
out/
.env.local
.env*.local
.env
.vercel
tsconfig.tsbuildinfo
# Playwright auth state (contains JWT — never commit)
tests/.auth/
# Playwright test artifacts
test-results/
playwright-report/

# OpenNext / Cloudflare Workers build output (never commit — contains baked env)
.open-next/
.wrangler/
