# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.project
.classpath
.settings/
.vscode/

# Kotlin
*.kotlin_module
.kotlin/

# macOS
.DS_Store
*.swp
*~

# Local configuration
local.properties
*.local

# Logs
*.log

# Generated files
generated/

# Kotlin/Native
.konan/
*.kexe
*.klib

# Android
*.apk
*.aab
*.dex
*.class
captures/
.externalNativeBuild/
.cxx/

# iOS/Xcode
xcuserdata/
*.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xcuserstate
Pods/

# Test results
test-results/
test-output/

# Coverage
coverage/
*.exec
*.ec

# Detekt
build/reports/detekt/

# Spotless
.spotless/

# Secrets (never commit these!)
*.jks
*.keystore
*.gpg
google-services.json
*.p12
*.pem
secrets/

# Temporary files
tmp/
temp/
*.tmp
*.bak

# npm (if using Kotlin/JS)
node_modules/
package-lock.json
!docusaurus/package-lock.json
!dashboard/package-lock.json

# Documentation build
docs/_build/
site/

# PayCraft environment secrets — NEVER commit
.env
.env.local
.env.*.local
supabase/.env

# CLI build output
cli/dist/

# Local supabase CLI artifacts (supabase/.gitignore covers .temp + .branches;
# this catches the convenience symlink supabase/migrations → ../server/migrations
# that local devs may add so `supabase db reset` finds the migrations).
# NOTE: migrations that must be versioned + deployed are re-included below via `!`
# negation (a blanket ignore here silently dropped schema changes on deploy — see
# migration 075 / paycraft-provider-platform-onboarding epic). Track new schema by
# adding a matching `!supabase/migrations/NNN_*.sql` line.
supabase/migrations/*
!supabase/migrations/075_routing_platform_dimension.sql
!supabase/migrations/076_native_store_methods.sql
!supabase/migrations/077_tenant_paywall_trial_disclosure.sql
!supabase/migrations/078_tenant_products_sync_state.sql
!supabase/migrations/079_encryption_passphrase_prod_guard.sql
!supabase/migrations/080_coupon_store_offer_ids.sql
!supabase/migrations/081_realtime_foundation.sql
.vercel

# Wrangler CLI cache (Cloudflare Workers)
.wrangler/
!supabase/migrations/082_rls_security_hardening.sql
!supabase/migrations/083_rpc_tenant_ownership_guards.sql
!supabase/migrations/084_rpc_guard_allow_backend.sql
