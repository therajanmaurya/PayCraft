# AC-7 (RED) — backfill-unmappable-product

Widens the interval CHECK to simulate someone adding a cadence and forgetting the backfill's CASE; 088 must abort naming the row rather than leave it unmapped. Restores the constraint on exit.

Run: `bash run.sh`
