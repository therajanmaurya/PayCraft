# AC-30 — pricing-page-no-stub

The rendered pricing page shows the real product; the retired Product/999 stub literals are absent. Needs a browser — the page fetches client-side.

Run: `bash run.sh`
