# AC-12 / AC-15 — price-chain-shadow

Both price-country chains, exercised through the shipped resolver. Includes the Stage A invariant swept across every platform/locale/geo combination: served is always locale-or-override.

Run: `bash run.sh`
