# AC-19 — cross-chain-equivalence

One fixture drives both the dashboard web-checkout chain and the edge shadow chain; country and provenance must match, so a buyer is not priced two ways.

Run: `bash run.sh`
