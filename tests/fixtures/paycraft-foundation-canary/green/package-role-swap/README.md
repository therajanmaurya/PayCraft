# AC-6 — package-role-swap

A paywall bound to a package ROLE keeps resolving after the underlying SKU is swapped — the reason the Offering → Package → Product indirection exists at all.

Run: `bash run.sh`
